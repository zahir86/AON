<?xml version="1.0" encoding="UTF-8"?>
<!--
  AON_PERF_checkStuckProcesses
  Identifica i processi attivi bloccati da più di N minuti.
  Chiamata dal Process Model schedulato (AON_PERF_ScheduledMonitor)
  e usata anche dalla sezione alert della dashboard.

  INPUT:
    ri!thresholdMins      - Soglia in minuti (default: cons!AON_PERF_STUCK_THRESHOLD_MINS)
    ri!processModelNames  - Lista nomi critici da controllare (default: cons!AON_PERF_MONITORED_PROCESS_MODELS)

  OUTPUT: Dictionary {
    hasStuck     : Boolean
    stuckCount   : Integer
    stuckList    : List of {
                     processName,
                     processModelName,
                     startTime,
                     runningMins,
                     initiator
                   }
    checkedAt    : DateTime
  }
-->
<ae:expressionRule xmlns:ae="http://www.appian.com/ae/types/2009"
                   name="AON_PERF_checkStuckProcesses"
                   description="Identifica processi ACTIVE bloccati oltre la soglia configurata. Usato dal monitor schedulato e dal pannello alert della dashboard.">

  <ae:inputs>
    <ae:input name="ri!thresholdMins"     type="?Integer"/>
    <ae:input name="ri!processModelNames" type="List of Text String"/>
  </ae:inputs>

  <ae:expression><![CDATA[
/* ===========================================
 * AON_PERF_checkStuckProcesses
 *
 * Cerca processi con status=ACTIVE il cui
 * startTime è più vecchio di N minuti.
 * Un processo ACTIVE da troppo tempo è
 * probabilmente bloccato (es. invio mail fermo).
 * =========================================== */
a!localVariables(
  local!threshold: if(
    isnull(ri!thresholdMins),
    cons!AON_PERF_STUCK_THRESHOLD_MINS,
    ri!thresholdMins
  ),

  /* Lista processi critici da JSON constant (se ri! non fornito) */
  local!monitoredModels: if(
    not(isnull(ri!processModelNames)) & length(ri!processModelNames) > 0,
    ri!processModelNames,
    fromjson(cons!AON_PERF_MONITORED_PROCESS_MODELS)
  ),

  /* Data/ora soglia: processi avviati prima di questo momento */
  local!cutoffTime: now() - (local!threshold / (24 * 60)),

  /* Query per processi ACTIVE con startTime < soglia */
  local!queryResult: a!queryProcessAnalytics(
    processAnalyticsReport: a!processAnalyticsReportById(
      id: cons!AON_PERF_PAR_EXECUTION_TIMES
    ),
    query: a!query(
      selection: a!querySelection(
        columns: {
          a!queryColumn(field: "c1"),   /* processName      */
          a!queryColumn(field: "c2"),   /* processModelName */
          a!queryColumn(field: "c3"),   /* startTime        */
          a!queryColumn(field: "c7")    /* initiator        */
        }
      ),
      filter: a!queryLogicalExpression(
        operator: "AND",
        filters: {
          /* Solo processi ancora attivi */
          a!queryFilter(field: "c6", operator: "=", value: "ACTIVE"),
          /* Avviati prima della soglia (quindi attivi da troppo tempo) */
          a!queryFilter(field: "c3", operator: "<=", value: local!cutoffTime),
          /* Filtra solo i modelli critici monitorati */
          if(
            length(local!monitoredModels) > 0,
            a!queryFilter(
              field: "c2",
              operator: "in",
              value: local!monitoredModels
            ),
            {}
          )
        }
      ),
      pagingInfo: a!pagingInfo(startIndex: 1, batchSize: 500)
    )
  ),

  local!rows: index(local!queryResult, "data", {}),

  /* Costruisce lista dettagliata con minuti trascorsi */
  local!stuckList: a!forEach(
    items: local!rows,
    expression: a!localVariables(
      local!start: index($item, "c3", null()),
      local!runningMins: if(
        isnull(local!start),
        null(),
        round(
          (dateTimeToMilliseconds(now()) -
           dateTimeToMilliseconds(local!start)) / 60000,
          0
        )
      ),
      {
        processName:      index($item, "c1", ""),
        processModelName: index($item, "c2", ""),
        startTime:        local!start,
        runningMins:      local!runningMins,
        initiator:        index($item, "c7", "")
      }
    )
  ),

  /* --- Output --- */
  {
    hasStuck:   length(local!stuckList) > 0,
    stuckCount: length(local!stuckList),
    stuckList:  local!stuckList,
    checkedAt:  now()
  }
)
  ]]></ae:expression>

</ae:expressionRule>
